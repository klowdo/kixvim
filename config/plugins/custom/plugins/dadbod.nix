{
  # https://nix-community.github.io/nixvim/plugins/toggleterm/index.html
  plugins = {
    vim-dadbod = {
      enable = true;
    };

    vim-dadbod-completion = {
      enable = true;
    };
    vim-dadbod-ui = {
      enable = true;
    };
  };
}
